__pycache__/
*.pyc
.venv/
venv/
.env
# Track JSON + TXT reports so they sync to the cloud dashboard via GitHub.
# Keep diagnostic PNGs/HTML out of git (large, noisy, regenerable).
reports/probe_*.png
reports/probe_*.html
reports/listing_*.png
reports/listing_*.html
reports/run_*.png
reports/dc_code_debug.*
reports/cart_with_code.html
reports/homepage.png
reports/flash_*.png
reports/diagnostics/
!reports/.gitkeep
.playwright/
# Saved admin category-grid HTML pages — large, regenerable, not committed.
# Drop your page_*.html exports here and run build_category_catalog.py
category_pages/*.html
config/category_catalog.xlsx
